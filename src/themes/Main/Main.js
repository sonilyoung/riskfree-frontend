import { createTheme } from '@mui/material/styles';

const main = createTheme({
    typography: {
        headline1: {
            fontWeight: '300',
            fontSize: '100px',
            lineHeight: '112px',
            letterSpacing: '-1.5px'
        },
    },
});

export default main;

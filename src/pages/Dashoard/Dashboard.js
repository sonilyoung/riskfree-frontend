import React from 'react';
import { DefaultLayout } from '../../layouts/Default';

const Dashboard = () => {
    return (
        <DefaultLayout>
            I am dashboard page :-) 
        </DefaultLayout>
    );
};

export default Dashboard;
